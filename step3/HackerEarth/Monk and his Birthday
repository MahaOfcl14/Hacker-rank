#include <bits/stdc++.h>
using namespace std;
int main() {
    int T;
    cin>>T;
    while(T--){
        int N;
        cin>>N;
        set<string>InviteList;
        for(int i=0; i<N; i++){
            string name;
            cin>>name;
            InviteList.insert(name);

        }
        for(const string& name : InviteList){
            cout<<name<<endl;
        }
    }
    return 0;
	
}
