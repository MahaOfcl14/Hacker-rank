string angryProfessor(int k, vector<int> a) {

    int cnt = 0;
    string ans = "";
    for(int i=0; i<a.size(); i++){
        if(a[i]<=0) cnt++;
    }
    if(cnt >= k) ans="NO";
    else ans="YES";
    return ans;

}
